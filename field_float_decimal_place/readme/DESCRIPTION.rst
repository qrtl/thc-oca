This module allows administrators to define the decimal precision of float fields based on
the model. PDF reports generated from these models will also reflect the specified decimal
precision.

Note: It only changes the presentation in the user interface and does not alter the data
stored in the database.
