Go to *Settings > Technical > User Interface > Field Float Decimal*, and create records
according to your needs.

For each record:

- Set **Model** and **Field** (required)
- Set Company (optional)
- Set **Digits**. Defines the number of decimal places to be used for the specified model and field.
